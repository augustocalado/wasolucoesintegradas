export const metadata = {
  title: 'Painel Administrativo | WA Soluções Integradas',
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
